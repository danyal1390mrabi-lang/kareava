# راهنمای سایت کار اوا (kareava.ir)

## ساختار فایل‌ها
```
index.html          صفحه‌ی اصلی سایت (لیست و جستجوی آگهی‌ها)
support.html        صفحه‌ی پشتیبانی (بله و روبیکا)
styles.css           استایل مشترک صفحات آگهی و پشتیبانی
data/listings.json   ⭐ منبع اصلی همه‌ی آگهی‌ها — همینجا آگهی اضافه می‌کنی
ad/                  صفحات جداگانه‌ی هر آگهی (خودکار ساخته می‌شن، دست نزن)
scripts/build.js      اسکریپتی که لینک و صفحه‌ی هر آگهی رو خودکار می‌سازه
sitemap.xml           نقشه‌ی سایت برای گوگل (خودکار ساخته می‌شه، دست نزن)
robots.txt             (خودکار ساخته می‌شه، دست نزن)
CNAME                  دامنه‌ی سفارشی گیت‌هاب پیجز (kareava.ir)
.github/workflows/     تنظیمات اجرای خودکار روی گیت‌هاب
```

## چطور یه آگهی جدید اضافه کنم؟ (تا ۵۰۰ تا و بیشتر)
۱. فایل `data/listings.json` رو باز کن.
۲. یک آیتم جدید مثل نمونه‌ی زیر به آرایه اضافه کن:

```json
{
  "id": "s33",
  "title": "عنوان دقیق و واضح آگهی — همون کلمه‌ای که مشتری در گوگل سرچ می‌کنه",
  "price": 0,
  "category": "construction",
  "city": "تهران",
  "img": "نام-فایل-عکس.jpg",
  "phone": "09121234567",
  "desc": "توضیح کامل خدمات، بدون کپی از سایت‌های دیگه",
  "createdAt": 1735000000000
}
```

- `id` باید برای هر آگهی یکتا باشه (مثلاً s33, s34, ...).
- `category` یکی از این‌ها: construction, auto, beauty, tech, home, education, food, art, health, other
- عکس آگهی (فایل `img`) رو باید در همون پوشه‌ی ریشه‌ی سایت (کنار index.html) آپلود کنی.
- `createdAt` رو می‌تونی خالی بذاری، خودکار پر می‌شه.

۳. تغییرات رو **Commit و Push** کن.
۴. همین! گیت‌هاب اکشن به‌صورت خودکار:
   - یک لینک اختصاصی سئو-فرندلی برای آگهی می‌سازه (مثلاً `/ad/کار-نرده-سازی-یزد.html`)
   - یک صفحه‌ی کامل و مجزا با عنوان، توضیحات و اطلاعات ساختاریافته (Schema.org) برای اون آگهی می‌سازه که گوگل می‌تونه جدا ایندکسش کنه
   - `sitemap.xml` رو به‌روزرسانی می‌کنه

**نکته‌ی مهم برای دیده‌شدن در گوگل:** عنوان (`title`) و توضیحات (`desc`) رو دقیقاً با همون کلماتی بنویس که مشتری‌های واقعی سرچ می‌کنن (مثلاً «نرده سازی یزد» یا «نصب نرده استیل»)، چون این متن مستقیم توی `<title>`، توضیح متا و متن صفحه قرار می‌گیره و باعث میشه گوگل اون آگهی رو برای همون عبارت بالا بیاره.

## راه‌اندازی دامنه‌ی kareava.ir روی گیت‌هاب پیجز
۱. در تنظیمات DNS دامنه (جایی که دامنه رو خریدی)، این رکوردها رو اضافه کن:
   - چهار رکورد A برای ریشه‌ی دامنه (@) به آی‌پی‌های گیت‌هاب پیجز:
     `185.199.108.153` , `185.199.109.153` , `185.199.110.153` , `185.199.111.153`
   - یا اگر می‌خوای با www هم کار کنه، یک رکورد CNAME برای www به `<username>.github.io`
۲. در ریپازیتوری گیت‌هاب: Settings → Pages → بخش Custom domain → بنویس `kareava.ir` → Save.
   (فایل CNAME از قبل توی ریشه‌ی پروژه گذاشته شده، پس گیت‌هاب خودش تشخیص می‌ده.)
۳. تیک «Enforce HTTPS» رو بزن (ممکنه چند دقیقه تا چند ساعت طول بکشه تا فعال بشه).
۴. بعد از فعال شدن، سایت‌مپ رو به گوگل سرچ کنسول معرفی کن:
   Google Search Console → Sitemaps → آدرس `https://kareava.ir/sitemap.xml` رو ثبت کن.
   این کار به گوگل کمک می‌کنه همه‌ی ۵۰۰ آگهی رو سریع‌تر پیدا و ایندکس کنه.

## پشتیبانی
لینک بله و روبیکا در فوتر و هدر سایت و در صفحه‌ی `support.html` قرار داره:
- بله: poshtibany12@
- روبیکا: Sales_Support_a@

## اجرای دستی build.js (اختیاری، برای تست روی سیستم خودت)
```
node scripts/build.js
```
